# Sparse Vector Library

Библиотека для работы с разреженными векторами на C.

## Функции

- `sparse_vector_create()` - создание вектора
- `sparse_vector_dot()` - скалярное произведение
- `sparse_vector_to_dense()` - восстановление плотного вектора
- `sparse_vector_add()` - сложение векторов
